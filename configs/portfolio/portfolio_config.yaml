dataset:
  path: data/out_cut.parquet
  date_column: date
  id_column: permno
  return_column: ret_firm # your realized excess return column
  signal_column: forecast_score # your model’s score column

portfolio:
  n_quantiles: 10
  long_quantile: 10
  short_quantile: 1

output:
  dir: portfolios/results
